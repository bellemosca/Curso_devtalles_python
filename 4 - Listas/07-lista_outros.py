num = list(range(100))
# print(num)

setence = " ".join(["oi", "mundo,", "tudo", "bem", "com", "vocês", "?"])
print(setence)

total = sum(num)
maior = max(num)
menor = min(num)
elementos = len(num)

print(elementos)
